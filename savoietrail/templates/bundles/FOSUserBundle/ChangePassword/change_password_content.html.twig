{% trans_default_domain 'FOSUserBundle' %}

<h2 class="text-center">Changer de mot de passe<img src="/images/password.png" class="icone ml-5" alt="icone"/></h2>

{{ form_start(form, { 'action': path('fos_user_change_password'), 'attr': { 'class': 'fos_user_change_password' } }) }}
    {{ form_widget(form) }}
    <div>
        <input class="btn-primary btn" type="submit" value="{{ 'change_password.submit'|trans }}" />
    </div>
{{ form_end(form) }}
